import React from 'react';
import withLoading from './withLoading';

const FetchingData = () => {
    return (
        <div>
            FetchingData
        </div>
    );
};

export default withLoading(FetchingData);